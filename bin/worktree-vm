#!/usr/bin/env bash
# worktree-vm -- give the current git worktree its own Colima VM and run the
# project's build/test commands inside it. Project-agnostic; driven by a
# worktree-vm.conf at the consuming repo root.
#
# Usage: worktree-vm <command> [args]
set -euo pipefail

# resolve our own location (following symlinks) to find lib/.
_src="${BASH_SOURCE[0]:-$0}"
while [ -h "$_src" ]; do
  _dir=$(cd -P "$(dirname "$_src")" >/dev/null 2>&1 && pwd)
  _src=$(readlink "$_src")
  case "$_src" in /*) ;; *) _src="$_dir/$_src" ;; esac
done
SELF_DIR=$(cd -P "$(dirname "$_src")" >/dev/null 2>&1 && pwd)
LIB_DIR="$SELF_DIR/../lib"

# shellcheck source=lib/common.sh
. "$LIB_DIR/common.sh"
# shellcheck source=lib/commands.sh
. "$LIB_DIR/commands.sh"

usage() {
  cat >&2 <<'EOF'
worktree-vm -- one ephemeral Colima VM per git worktree.

Commands (operate on the CURRENT git worktree):
  provision     Create/start the worktree's VM; install toolchain; seed files.
  up            provision + run CMD_UP inside the VM + wait for health.
  test [args]   Run CMD_TEST [args] inside the VM (localhost = the stack).
  tunnel [t]    Surface an in-VM port to the host (t = ui|api|<port>, default ui).
  untunnel      Close all tunnels for this worktree's VM.
  status        Show VM status, containers, health, and open tunnels.
  ssh [cmd]     Shell into the VM (or run a command in the worktree dir).
  down          Stop the VM (keeps disk).
  destroy [-f]  Delete the VM entirely.
  name          Print the VM/profile name for this worktree.

Config: <repo-root>/worktree-vm.conf (copy worktree-vm.example.conf).
EOF
}

main() {
  local cmd="${1:-}"; [ "$#" -gt 0 ] && shift || true

  case "$cmd" in
    ""|help|-h|--help) usage; exit 0 ;;
    version|--version) printf 'worktree-vm %s\n' "$WTVM_VERSION"; exit 0 ;;
  esac

  resolve_repo
  load_config
  VM=$(vm_name)

  case "$cmd" in
    provision) cmd_provision ;;
    up)        cmd_up ;;
    test)      cmd_test "$@" ;;
    tunnel)    cmd_tunnel "${1:-ui}" ;;
    untunnel)  cmd_untunnel ;;
    status)    cmd_status ;;
    ssh)       cmd_ssh "$@" ;;
    down)      cmd_down ;;
    destroy)   if [ "${1:-}" = "-f" ]; then WTVM_FORCE=1; fi; cmd_destroy ;;
    name)      printf '%s\n' "$VM" ;;
    *)         die "Unknown command: '$cmd' (try 'worktree-vm help')." ;;
  esac
}

WTVM_VERSION="0.1.0"
main "$@"
